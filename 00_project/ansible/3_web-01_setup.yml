---
- name: Web-01 setup
  hosts: web-01
  become: true

  tasks:
    - name: Install usefull packages
      ansible.builtin.apt:
        pkg:
          - pwgen
        state: present
        update_cache: yes

  roles:
    - docker_install
    - seafile_install
